# ─── Web Framework ────────────────────────────────────────────────────────────
fastapi==0.136.1
uvicorn==0.47.0
starlette==1.0.0
python-multipart==0.0.29        # required for OAuth2PasswordRequestForm in Swagger

# ─── Database ─────────────────────────────────────────────────────────────────
sqlalchemy==2.0.49
asyncpg==0.31.0                 # async PostgreSQL driver
psycopg2-binary==2.9.12         # sync driver for Alembic migrations
alembic==1.18.4
greenlet==3.5.0                 # required by SQLAlchemy async

# ─── Auth & Security ──────────────────────────────────────────────────────────
python-jose[cryptography]==3.5.0  # JWT tokens
passlib==1.7.4                    # password hashing
bcrypt==4.0.1                     # pinned — passlib 1.7.4 breaks with bcrypt 4.1+
cryptography==48.0.0

# ─── Config & Validation ──────────────────────────────────────────────────────
pydantic==2.13.4
pydantic-settings==2.14.1
pydantic-core==2.46.4
python-dotenv==1.2.2
email-validator==2.3.0            # required for EmailStr in Pydantic

# ─── HTTP & Async ─────────────────────────────────────────────────────────────
anyio==4.13.0
h11==0.16.0
idna==3.15
dnspython==2.8.0

# ─── Templating (Alembic uses Mako) ───────────────────────────────────────────
mako==1.3.12
markupsafe==3.0.3

# ─── Utils ────────────────────────────────────────────────────────────────────
click==8.4.0
colorama==0.4.6
typing-extensions==4.15.0
typing-inspection==0.4.2
annotated-types==0.7.0

# ─── Phase 6 — add when starting Phase 6 ─────────────────────────────────────
# cloudinary
# celery[redis]
# redis
# qdrant-client

# ─── Phase 7 — add when starting Phase 7 ─────────────────────────────────────
# pdfplumber
# python-docx
# python-pptx
# openpyxl
# tiktoken

# ─── Phase 8 — add when starting Phase 8 ─────────────────────────────────────
# anthropic
# openai
# sentence-transformers

# ─── Phase 9 — add when starting Phase 9 ─────────────────────────────────────
# fastapi-mail
